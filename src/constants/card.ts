import type { CardOptions } from '@/types';

/**
 * Default options for the card customization.
 *
 * @see CardOptions
 */
export const DEFAULT_CARD_OPTIONS: CardOptions = { color: '#12b848', couplet: '', loading: false };
