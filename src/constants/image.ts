/**
 * Configuration for card image generation.
 *
 * @property element_id - The DOM element ID used for image capture
 * @property scale_factor - The multiplier for image resolution (higher = better quality)
 */
export const CARD_IMAGE = { element_id: 'doha-preview', scale_factor: 6 } as const;
