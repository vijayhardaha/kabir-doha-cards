/**
 * Lists the supported variants for toast notifications.
 */
export type ToastType = 'success' | 'error';

/**
 * Lists the supported platforms for outbound share links.
 */
export type Platform = 'whatsapp' | 'wa' | 'twitter' | 'x';
